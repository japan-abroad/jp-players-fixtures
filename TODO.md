# 実装予定・検討事項

現時点では未実装だが、将来追加する候補として記録しておくもの。

## 機能追加

- **試合結果の表示**: 現在は今後の試合予定のみ表示。過去の試合結果(スコア)も
  表示できるようにする。API-Football側は同じ `/fixtures?date=` エンドポイントで
  過去日付を指定すれば `goals` フィールドにスコアが入るため技術的には対応可能。
  対応時は取得期間(過去何日分か)と、予定/結果が混在した際の一覧上の見た目を
  要検討。(2026-08-25 ユーザーより要望、優先度は保留)

## 要検証

- `scripts/config.py` の Championship(イングランド2部)のAPI-Football
  リーグID(`40`)は記憶ベースで設定したもの。無料枠が復活し
  `/leagues?name=Championship&country=England` が叩ける状態になったら
  正しいIDか確認すること。

## 未実装のまま残している箇所

- `src/components/AffiliateSlot.tsx`: アフィリエイト/広告枠のプレースホルダー。
  提携先が決まるまで意図的に何も表示しない実装のまま。
